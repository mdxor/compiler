use crate::block;

// pub fn parse(code: &str) -> Vec<String> {
//   let mut parser = Parser::new(code);
//   let mut tokens = vec![];
//   loop {
//     if let Some(token) = parser.next_token() {
//       tokens.push(token)
//     } else {
//       break;
//     }
//   }
//   tokens
// }
